from app.services.base import BaseService


class ModelService(BaseService):
    def calculate_relevance(self, training_data: list[float]) -> list[float]:
        """Calculate relevance"""
        return []
